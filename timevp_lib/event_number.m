function e_num = event_number(event)
%event_number counts the number of occurrences of the given event
%  Usage: event_number(EVENT_DATA)
%  example: event_number([1 5; 10 20]) % returns 2.
e_num = size(event,1);

